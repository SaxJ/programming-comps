#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;
    for (int t = 0; t < T; t++) {
        int R, C;
        cin >> R >> C;

        string wall[R];
        for (int i = 0; i < R; i++)
            cin >> wall[i];
    }
}
